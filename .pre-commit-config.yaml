repos:
  - repo: local
    hooks:
      - id: commitizen-commit-msg
        name: commitizen commit message check
        entry: uv run cz check --commit-msg-file
        language: system
        stages: [commit-msg]
